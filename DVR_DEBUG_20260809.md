# DVR 复现调试记录（2026-08-09）

## 本轮目标与判据

本轮固定比较 Camel、GAP BFS 和 Kangaroo 的 baseline/full。先验收逻辑，再做性能优化。

正确性必须同时满足：

1. baseline/full 使用同一 ELF、输入和 cache/core 配置，程序结果一致；
2. committed instructions 守恒（带 `m5_reset_stats/dump_stats` 时比较同一 ROI block）；
3. helper `decoded == issued == completed`；
4. 共享 LSQ 路径满足 `allocated == completed + faults + retries + dropped + pending`；
5. dependent request `generated == issued == completed`；
6. translation fault、replay reference mismatch、VIR active-mask failure、NDM flatten failure 均为 0；
7. PC 级 cache 事件能把 trigger/source/dependent 地址和 demand miss 的变化闭环对应起来。

性能统一使用 gem5 模拟的 ROI `simTicks`，speedup 定义为 `baseline_ticks / dvr_ticks`。
墙钟时间仅用于监控实验，不进入性能结论。

## 冻结输入

- gem5 仓库提交：`b1ecebd5516805f3b821d73f5fcb23f190592da4`，另有本轮未提交 DVR 修改。
- Camel MAX_KEY=4096 ELF SHA256：`cee46bfe2bb9b0470f97ffee422663b9c52477d4b76b326bec951e18e7cf89e9`。
- GAP BFS：GAPBS `2972aeb2703165bafd921222f4ed7196f542d3a8`，`SERIAL=1`、静态 RISC-V、`-O3 -fno-tree-vectorize`，ELF SHA256：`cc568ed7fb349ca3f927cc639d1df2cfd0aabd15a105f381fe798bec6cd83059`；输入 `-g 10 -n 1`。
- Kangaroo k0/full-ROI ELF SHA256：`19a3fa27677d4818fac999df63af303dad0b22eb2add0d3258adcb14a786e186`；Size=131072、Arrays=8、Iterations=2。
- 结果根目录：`/home/lynnhoo/dvr-repro/results/paper-validation-20260809`。
- full 参数：`--dvr --dvr-mode=full --dvr-vector-chunks --dvr-quality-probe`；共享物理寄存器文件为默认配置。

## Camel 第一轮（MAX_KEY=4096）

程序结果 baseline/full 均为 `Result 8006810`。ROI committed instructions 均为 327809。

| 指标 | Baseline | Full |
|---|---:|---:|
| ROI simTicks | 41,090,250 | 40,622,250 |
| ROI IPC | 1.994445 | 2.017423 |
| speedup | 1.000x | 1.0115x |
| L1D demand misses（全 ROI） | 1096 | 1102 |
| source PC `0x103b4` demand misses | 970 | 877 |
| dependent PC `0x103ba` demand misses | 109 | 86 |

关键链的 demand miss 共减少 116，但全 ROI miss 增加 6，说明 DVR 对目标链有效，同时存在
非目标 PC 的 cache 扰动；不能只看 aggregate miss 数判断机制无效。Full 中 helper 动态 uop
为 `48/48/48`（decode/issue/complete）；共享 VRAT program 为 1；helper load entry 为
`allocated 250 = completed 234 + retry 16`，fault/drop/pending 均为 0，peak shared-LSQ
in-flight 为 79。Discovery 只启动 1 代，随后 4126 次候选因 active helper 被阻止。

quality probe 记录 234 个请求被 L1D 接收、178 条 request line 完成、35 条形成 DVR fill；
34 条及时使用、1 条 late、0 条 unused eviction，fill accuracy/timeliness 均为 97.14%。
shadow-cache coverage 为 `32/2496 = 1.2821%`。因此本轮逻辑路径通过，但低 coverage 和
单代 helper 生命周期是主要性能瓶颈。

## 进行中

- BFS baseline 已完成：图为 1024 nodes / 10496 undirected edges，正常退出；full 仍在运行。
- Kangaroo baseline/full 均已进入正式 kernel，正在运行。

## Helper lifecycle 调试与修复

第一轮 Camel 出现一个不合理组合：helper dyn-uop 已经 `48/48/48`，共享 LSQ pending=0，
但 `DiscoveryStarts=1` 且后续 4126 个候选全部被 active helper 阻止。逐层加入重叠阻塞计数后，
定位过程如下：

1. 最初 4126/4126 次阻塞都有 frontend 未完成，而 compute 仅 5 次、LSQ 仅 8 次、VIR 为 0；
2. load uop 在实现中被正确拆成 scalar lanes 进入共享 LSQ，但独立 8-entry frontend 项未被释放；
3. 把 load 的退休绑定到 LSQ issue 仍不足，因为 source lane 可能早于对应 frontend fetch/decode；
4. 重新按论文结构划分所有权：8-entry 是 fetch/decode staging buffer，decode 后即移交 VIR，
   load 则移交共享 LSQ，执行等待不能继续占 frontend；此后 frontend 长期阻塞从 4126 降至 83；
5. 剩余 4120 次来自孤立 `readyUops` credit。该计数是仲裁 token，不拥有真实工作；一个 decoded
   vector load fan-out 成多个 scalar LSQ lanes 后，token 数与 lane 数本来就不一一对应；
6. 最终 helper 结束条件改为 frontend fetch/decode、VIR、compute/replay、issued memory、CPU request
   queue 与共享 LSQ 全部清空。只有这些真实所有者全空时才清除残留 token 并进入 Idle。

最终 Camel ROI 结果：

| 指标 | Baseline | 修复后 Full |
|---|---:|---:|
| ROI committed | 327809 | 327809 |
| ROI simTicks | 41,090,250 | 36,843,250 |
| ROI IPC | 1.994445 | 2.224349 |
| speedup | 1.0000x | 1.1153x |
| Discovery starts | 0 | 790 |
| helper launch admitted | 0 | 790 |
| helper dyn uop decode/issue/complete | 0 | 583/583/583 |
| dependent generated/issued/completed | 0 | 1172/1172/1172 |
| source PC `0x103b4` demand misses | 970 | 118 |
| dependent PC `0x103ba` demand misses | 109 | 32 |

共享 LSQ 守恒为 `3092 allocated = 2836 completed + 256 retry`，fault/drop/pending 均为 0；
replay mismatch 与 VIR active-mask failure 均为 0。关键链 demand miss 共减少 929，ROI speedup
达到 1.1153x。quality probe 的 fill accuracy/timeliness 均为 100%，coverage 提升到 7.38%；
仍有 177 次 pollution eviction，但没有形成观测到的 pollution miss。

## BFS ROI 口径修正

第一轮普通 GAP BFS 全程序对照会把图生成、模拟时间读取、打印和退出代码计入 committed；不同
cache timing 会改变计时/格式化路径，因而 full 与 baseline 相差少量 committed instruction。
这不能直接作为 BFS kernel 错误证据。后续改用 `bfs_roi_stats.riscv -g 10 -n 1 -v`：

- `m5_reset_stats()` 位于 BFS traversal 前；
- `m5_dump_stats()` 位于 parent cleanup 前；
- `-v` 运行独立串行 BFS verifier，并要求输出 `Verification : PASS`；
- 性能与 committed 只取 stats 第一段。

首轮 ROI baseline verifier 已 PASS，ROI 为 57,857 committed、17,042,750 ticks。首轮 full 使用
生命周期修复前的二进制，ROI reset 后仍继承初始化阶段卡住的 helper，故结果标为无效；正在用
最终生命周期修复重跑。

## Kangaroo 进度

Baseline 已完整跑完两次 iteration，ROI 第一段为 25,178,929 committed、7,891,683,000 ticks、
IPC 0.797641、L1D demand miss 1,465,956。长运行已经跨过旧实现 native-IQ reservation assertion
的故障点。Full 仍在自然运行，结束后按 PC 对齐 miss 链与 dependent replay。

## Camel 正式输入（MAX_KEY=65536）

同一 ELF、Table-1 配置和 ROI 下：

| 指标 | Baseline | Full | 比值/说明 |
|---|---:|---:|---:|
| ROI committed | 5,767,296 | 5,767,296 | 完全一致 |
| ROI simTicks | 1,416,496,500 | 857,769,500 | **1.6514x speedup** |
| ROI IPC | 1.017880 | 1.680899 | 1.6514x |
| architectural demand miss（PC CSV） | 73,836 | 7,123 | -90.35% |
| source PC `0x103a4` demand miss | 15,394 | 44 | -99.71% |
| dependent PC `0x103aa` demand miss | 58,428 | 7,065 | -87.91% |

Full 的 source request 为 `16,689 L1 hit + 45,584 L1 miss = 62,273`，dependent request 为
`7,411 hit + 52,190 miss = 59,601`；两者之和 121,874，严格等于 shared-LSQ completed。
共享 LSQ 另有 1,120,699 次 main-port retry，因此 `1,242,573 allocated = 121,874 completed
+ 1,120,699 retry`。这些 retry 是主线程同周期优先的真实共享端口竞争，不是丢请求。

helper dyn-uop 为 `63,933/63,933/63,933`，dependent 为 `59,601/59,601/59,601`；
fault/drop/pending、replay mismatch、VIR active-mask failure 全为 0。quality probe：accuracy
98.35%、coverage 67.07%、timeliness 98.27%，pollution eviction 9,824，但仅 22 次形成
pollution miss。

## BFS scale10 的 PC 级解释

最终修复版使用 `bfs_roi_stats.riscv -g 10 -n 1 -v`，baseline/full verifier 都 PASS，ROI
committed 都是 57,857。ticks 为 17,042,750 / 17,507,500，即 full slowdown 2.73%。
helper `140/140/140`，shared-LSQ `480/480`，无 retry/fault/pending/mismatch；但 dependent
generated/issued/completed 全为 0。

反汇编和真实 L1D PC 事件显示，这个输入没有可观的 dependent-miss 机会：

- bottom-up 邻居读取 `0x118b2` 有 843 miss，但其后依赖 bitset load `0x118be` 只有 1 miss；
- top-down 邻居读取 `0x11970` 有 96 miss，但依赖 parent load `0x1197a` 为 0 miss；
- DVR 覆盖的是 CSR offset `0x118a2` 和 queue source `0x1194a`，source miss 有所下降；
- 额外 source traffic 产生 1,037 pollution eviction 和 53 pollution miss，超过本输入可获得的收益。

因此 scale10 的 slowdown 不是 cache-miss chain 地址错误，而是 workload working set 太小、关键
dependent loads 已命中。正在追加 scale12，以区分输入规模与 VTT/FLR 选择问题。

Kangaroo 第一轮 full（生命周期修复前）虽自然结束且不再触发 native-IQ assertion，但 ROI 内
`DiscoveryStarts=0`、helper activity=0、`BlockedByHelper=262166`，证明初始化遗留 helper 覆盖
整个 ROI；该轮 9,133,481,000 ticks 被标记无效。最终修复版 full 已重新启动。

### Kangaroo 最终 shared-128 正式结果

最终生命周期/VRAT 修正版与 baseline 都完成两次 iteration，ROI committed 都是 25,178,929。
`simTicks` 为 7,891,683,000 / 7,618,391,000，即 **1.0359x speedup**。helper uop 严格满足
`52,262/52,262/52,262`，shared-LSQ `181,756 allocated = 181,756 completed`，无 retry、
translation fault、active-mask failure 或 replay-reference mismatch。

但该结果只解决了 source，而没有解决 Kangaroo 用来检验的七级 dependent chain：source PC
`0x10878` demand miss 从 27,040 降至 8,242；随后 `0x108a8/0x108d6/0x10904/0x10932/
0x10960/0x1098e/0x109be` 的 baseline miss 分别为 219,468 / 214,466 / 208,321 /
203,269 / 201,141 / 193,387 / 191,775，full 反而略升为 220,009 / 214,965 / 209,002 /
203,949 / 201,958 / 194,209 / 192,591。dependent generated/issued/completed 全为 0。
旧统计中的 26,131 个 `SourceValueSemanticFailures` 需要用已加入的 scalar/vector PRF failure
拆分重跑判断；当前只能证明 source prefetch 带来 3.59% 净收益，不能声称间接链复现完成。

source-only ablation 与上述正式 full 在每个关键计数和 7,618,391,000 ticks 上逐项相同，确认正式
shared-128 中的 dependent 路径实际上完全没有贡献；3.59% 纯粹来自 source。

使用拆分统计的正式配置重跑进一步精确归因：48,054 次 helper 在创建 32-register scalar snapshot
时失败，成功建立的 1,835 个 VRAT 又发生 26,131 次 16-copy vector bundle failure；两者之和
74,185。真正的 unsupported semantic 为 0。也就是说旧的 26,131 semantic failure 全是错误分类，
而 Kangaroo dependent=0 是 scalar 与 vector 两级共享 PRF 压力共同造成。

只把 vector PRF 改为 256 后，dependent 路径被打开：`312,089/312,089/312,089`，其中
312,014 次主线程 dependent demand 命中了 CPU 记录的已完成目标，另有 12,453 次 late，证明
真实地址链能够覆盖后续 demand。该组 `ReplayReferenceChecks=347,865` 且全部 mismatch，是因为现有
“reference”把哈希/指针链错误地当成单一 trigger-value→address 仿射函数；它不是独立 scalar oracle，
也没有否决 recorded semantic replay。

然而 256-vec 组性能退化到 8,272,346,000 ticks（相对 baseline 0.9540x）：312,089 个 dependent
请求中 90,988 个 L1 hit、221,101 个 L1 miss，quality coverage 7.72%、accuracy 54.29%、
timeliness 89.44%，并产生 265,875 次 pollution eviction / 10,654 次 pollution miss。故当前关键
问题已经从“链没有执行”转为“128-lane 链过度超前且污染严重”；已启动 32/64-lane 节流实验。

成对的 int512 结果排除了 integer PRF 对主核的混淆：baseline 为 7,303,060,000 ticks；
vec128 full 为 7,059,403,000（1.0345x），scalar failure 为 0、但有 42,328 次 vector-bundle
failure，仍是 source-only；int512+vec256 消除两类 allocation failure 并发出 299,656 个 dependent，
但为 7,536,651,000 ticks（0.9690x）。后者覆盖 299,682 个 demand，却产生 235,674 pollution
eviction / 9,147 pollution miss。由此可确认：一旦资源足够，链功能会工作；剩余性能失败是预取距离/
流量控制，而不是 scalar 或 vector PRF 单项扩容本身。

为避免以后再次把哈希链误判为 replay 错误，affine checker 已改成候选关系建立后还必须由后续 3 个
scalar observation 连续验证，才允许计入 `ReplayReferenceChecks/Matches/Mismatches`；未验证关系计入
`ReferenceUnavailable`。该改动只修正诊断 oracle，不改变 recorded-uop replay 或请求地址。
Camel4096 重跑保持 36,843,250 ticks 和 1,172 个 dependent 不变；新 oracle 得到 1,424 checks、
1,424 matches、0 mismatch、128 unavailable，证明真实仿射链仍能通过独立检查。

## Shared VRAT 动态 rename 修复与资源敏感性

仓库 helper regression 的 divergent case 在 shared 模式下首先失败：helper dyn-uop 守恒，但
`DivergentBranches=0`，并且每个 VIR group 都对应一次 `SourceValueSemanticFailure`。代码检查发现
当前 `renameVector()` 为每个动态 vector destination 重新申请一整个 16-copy bundle。

这与论文 4.2.1 的明确描述不符：helper 是 in-order，不需要用每条指令的 rename 消除 WAW/WAR；
只有 vector source 写入尚未 vectorized 的 destination 时才首次分配 16 个 vector physical
registers。修改为“首次 `vectorize()`，后续按程序序原位写”后：

- Camel4096 的 semantic failure 从 194 降为 0，其他 ROI 统计和 1.1153x speedup 不变；
- Camel65536 的 semantic failure 从 21,311 降为 0，ROI ticks、PC miss、coverage 和 1.6514x
  speedup 与修复前逐项完全一致；
- divergent shared/128 从 0 提升到 999 个 divergent branch，但仍因共享 vector PRF 容量边界
  有 817 个后续 allocation/semantic termination，未形成 reconvergence；
- private-bank sensitivity 为 6,034 divergence / 1,844 reconvergence、0 semantic failure；
- shared 但 `numPhysVecRegs=256` sensitivity 为 4,042 divergence / 1,164 reconvergence、
  0 semantic failure。

默认仍保持论文 Table-1 的 128 physical vector registers。256 结果只证明 branch/reconvergence
逻辑在容量足够时可运行，不作为正式性能配置。RISC-V 主线程与 helper 同用 `VecElemClass`
free-list 后，可供 16-copy DVR mapping 使用的余量比 private ablation 小；这是真实共享资源压力，
不能通过悄悄回退 private bank 掩盖。后续需要把“无可用 16-copy bundle”独立统计为 VRAT resource
failure，并研究等待/终止策略，而不是归类为 unsupported semantic。

### BFS scale12（正式 shared-128 配置）

baseline/full 的 verifier 均 PASS，ROI committed 均为 333,140；`simTicks` 为
125,253,500 / 121,851,500，即 **1.0279x speedup**。source PC `0x118b2` 的 demand miss
从 11,699 降至 9,874；full 发出 17,692 个 source request（9,813 L1 hit、7,879 L1 miss）。
helper uop 为 `6,932/6,932/6,932`，shared-LSQ 为
`27,109 allocated = 17,692 completed + 9,417 retry`，无 translation fault、active-mask failure
或 replay-reference mismatch。

但该结果还没有验证 BFS 的间接 dependent 链：`0x118be` demand miss 仅 18→20，dependent
generated/issued/completed 均为 0。原统计把 3,466 次动态 destination 的 16-copy mapping 分配失败
错误归到 `SourceValueSemanticFailures`；这不是 ISA semantic 不支持，而是 shared-128 vector PRF 没有
完整 bundle。已把它拆成独立 `dvrVRATAllocationFailures`，正式容量仍保持 128，并启动 shared-256
sensitivity 验证“dependent=0”是否确由容量造成。该敏感性结果不替代论文配置。

shared-256 sensitivity 已闭环这个因果：dependent 变为 `1,626/1,626/1,626`，semantic failure
降为 0，说明 shared-128 的确限制了该切片继续 vectorize；但 1,626 个 dependent request 全部
是 L1 hit（0 miss），因此没有解决新的关键 miss，反而令 ROI 变成 123,373,750 ticks，只有
1.0152x speedup，弱于正式 shared-128 的 1.0279x。quality probe 的 pollution miss 也由 201
增至 218。结论是：容量限制是真实实现问题，但这个 scale12 输入即使解除限制，dependent 链也没有
可获益的 cache-miss；不能用盲目增大 PRF 冒充论文级性能改进。

补充的 BFS int512/vec128 资源拆分进一步表明：scalar snapshot failure 已为 0，但仍有 3,564 次
16-copy vector bundle failure，dependent 仍为 0；配对 baseline/full 是 126,665,000 /
121,421,250 ticks（1.0432x）。结合 int256/vec256 已能发出 1,626 个全 L1-hit dependent，可知
BFS-12 同时存在 scalar/vector PRF 压力，但无论解除哪一侧，本输入都没有值得覆盖的 dependent miss。

### RISC-V shared integer PRF 快照压力

新增的精确资源统计显示，Camel4096 正式 shared-128vec/256int 配置中，790 次 discovery 只有
13 次建立 VRAT；777 次均是初始化 RISC-V 32 个架构整数寄存器的新 scalar snapshot 时 free-list
不足，vector bundle failure 为 0。旧统计没有暴露这类失败。保持共享、仅把 integer PRF 改为 512
的 sensitivity 中，98/98 个 helper 全部建立，allocation failure 为 0，dependent 请求由 1,172
增至 2,679，DVR ROI ticks 从正式配置的 36,843,250 进一步降到 34,472,250。但配对的 512-int
baseline 自身也从 41,090,250 降到 36,923,250，因此同容量 speedup 只有 **1.0711x**；
1.1919x 是与 256-int baseline 的非配对比值，不能归因于 DVR。

论文的原始 ISA 只有 16-entry VRAT，却配置 256 integer physical registers；当前 RISC-V 端为 32 个
架构整数寄存器逐一申请 fresh scalar mapping，但仍照搬 256-entry PRF，因此没有保持同等快照余量。
512 组目前只作为端口归一化敏感性，不直接替换 Table-1 默认；需要由 BFS/Kangaroo 共同确认它是在
解除非预期端口瓶颈，而不是靠更大主核资源取得假加速。正在补 272/288/320 的成对扫点；272
对应“仅补偿 RISC-V 相比论文原 ISA 多出的 16 个 architectural integer names”的最小假设。

成对扫点结果如下（每一行都使用相同 integer PRF 的 baseline/full）：

| integer PRF | baseline ticks | DVR ticks | paired speedup | VRAT programs | scalar alloc failures |
|---:|---:|---:|---:|---:|---:|
| 256（正式） | 41,090,250 | 36,843,250 | 1.1153x | 13 | 777 |
| 272 | 39,450,250 | 35,919,250 | 1.0983x | 9 | 822 |
| 288 | 37,282,250 | 34,844,250 | 1.0700x | 25 | 494 |
| 320 | 36,876,250 | 34,330,250 | 1.0742x | 95 | 0 |
| 512 | 36,923,250 | 34,472,250 | 1.0711x | 98 | 0 |

320 已达到消除 scalar snapshot failure 的拐点，继续增至 512 没有收益；但配对 speedup 反而低于
正式 256，因为增大 PRF 也明显增强 baseline。故 320 适合证明 RISC-V 端口资源瓶颈及观察机制上限，
不应作为论文 Table-1 正式结果。真正的端口方案应明确 ISA-normalized PRF 预算或 helper reservation，
而不是只报告对 256 baseline 的非配对数字。

Kangaroo 的最小向量容量点与预期一致：int320/vec128 仍有 41,744 次 vector failure、dependent=0，
配对 speedup 1.0351x；只把 vec 增至 160 后 vector failure=0，dependent 约 312.4K，但
7,547,260,000 → 7,837,833,000（0.9629x），pollution miss 9,968。它证明从 128 增至 160
已经足以容纳该七级哈希链，不需要 256；性能依然为负再次把根因锁定到流量/距离而非容量。
该 sensitivity 在 ROI reset 边界有极少数 pre-ROI request 完成（generated/issued/completed 相差不超过
7），因此只用于容量与性能因果，不替代正式配置的严格 ROI 守恒结果。

### Kangaroo lane 节流结果（int320/vec160，共享 PRF）

配对 baseline 为 7,547,260,000 ticks。128 lanes 为 7,837,833,000（0.9629x）；64 lanes
降到 7,408,234,000（1.0188x），189,045 个 dependent 全部完成，accuracy 89.39%、coverage
4.59%、timeliness 71.55%，pollution miss 1,782；32 lanes 进一步降到 7,322,438,000
（**1.0307x**），124,409 个 dependent 全部完成，accuracy 90.06%、coverage 1.37%、timeliness
46.72%，pollution miss 仅 358。

因此 32 lanes 已证明七级链可以同时满足“真实 demand coverage、请求守恒、净加速”；但它仍略慢于
同配置 source-only 的 7,291,556,000（1.0351x）。32→64 提高 timeliness/coverage 却因流量和污染
降低性能。这里的 8/16/32/64 都是定位流量问题的 ablation，不是论文配置推荐；论文硬件仍固定为
最多128 scalar-equivalent lanes，并由 Discovery loop bound 屏蔽无效 lanes。若本 workload 的 inferred
bound ≥128，强制32就是偏离论文，只能作为诊断证据。
16 lanes 为 7,390,950,000（1.0211x），pollution miss 仅129，但 timeliness 降到37.54%；
因此少于32 lanes 已因不及时而丢失收益，32 是当前离散扫点的最佳链激活点。
8 lanes 进一步退化为 7,506,296,000（1.0055x），84,485 个 dependent 虽全部完成且 pollution
miss 仅21，但 timeliness 只有14.01%、coverage 0.20%。这确认性能曲线的下降来自 lookahead 不足，
而非地址链错误。

## Completed-target window 生命周期修复

BFS-14 第一轮虽然 verifier PASS、committed 与 baseline 同为 1,461,696，却只有 1/1,495 helper
launch admitted；其余 1,494 次均为 `DependentWindowStalls`。根因是图构建阶段完成、但 BFS kernel
永远不会消费的 dependent line 被永久计入 128-entry target window。论文中的窗口/LSQ 资源在响应完成
后应释放，completed line 只能作为去重/coverage 历史。

已把 capacity 改为只统计 queued+outstanding，并把 completed history 独立限制为 4,096 条。
Camel4096 回归完全不变：36,843,250 ticks、1,172/1,172/1,172 dependent、1,424/1,424
verified affine match、0 mismatch、0 pollution miss。BFS-12、BFS-14 和 Kangaroo-32 正在用修正版重跑。
BFS-12 也逐项不变：121,851,500 ticks、6,932/6,932/6,932 helper uop、
`27,109 = 17,692 completed + 9,417 retry`、pending=0；说明该修复只影响真正被 stale completed
history 卡住的 workload。

Kangaroo-32 的修正版从旧窗口的 7,322,438,000 进一步降到 7,204,343,000（相对同容量 baseline
**1.0476x**）：18,324 次 launch 全部 admitted，1,062,415 个 dependent 严格生成/发出/完成，
覆盖 704,395 个 demand；accuracy 92.40%、coverage 10.53%、timeliness 86.40%，pollution miss
4,047。它证明“永久 completed 占槽”既是 BFS phase-deadlock，也限制了同一链的有效 coverage。
但32仍是 ablation；已追加128-lane修正版，最终宽度结论以128为准。

## NDM 与 LBD/VTT 回归（VRAT 修复后）

固定边界 nested/NDM 用例完成：`dvrNDMAttempts=494`、`OuterFound=494`、
`BranchInversions/IR/ILR/LCR captures=494`、`OuterInvocations=988`，没有 fallback 或 timeout。
flatten 路径满足 `1,712,857 actual = 1,712,857 expected`，13,444 次 invariant check、0 failure；
dependent 请求为 `34,844/34,844/34,844`。可变边界用例同样满足
`373,798 actual = 373,798 expected`，3,265 次 invariant check、0 failure，其中 3,248 个 batch
确实包含不同 lane bound；dependent 为 `7,869/7,869/7,869`，translation fault 为 0。

独立 LBD/VTT regression 通过：58 次 VTT match、3,575 次安全 fallback、0 rollback。
这三组证据分别覆盖了边界捕获、outer×inner flatten 上限守恒、可变 lane mask 和 VTT 选择路径。

需要保留一个尚未关闭的问题：nested 的 `dvrReplayReferenceMismatches` 很高。当前所谓 reference
是 trigger-value 到 load-address 的单一 affine 训练关系，而 nested/pointer replay 实际执行的是记录的
多步依赖切片；因此该计数目前不能作为独立真值，且代码已禁止它否决有效的 recorded replay。
在将它改成逐条 scalar trace oracle 之前，不能把 mismatch=0 当作 nested 正确性的必要条件；最终报告会把
flatten/请求守恒的“已验证”和 replay-address oracle 的“未完全验证”分开陈述。
