## 输入
帮我调研一个课题。linux大小核架构、采用eas调度策略，小核的运行空闲时间占比相关的实验或者论文。可以是论文中的实验/也可以是别人自己在linux上做的实验，那你给我运行的app。最好近几年，如果没有的话，你就给我实验设置，运行的代表性app和linux verison和config，如何编译，我自己来跑。现在开始调研。一直自己运行，直至调研出一个结果，同时自己追问自己，调研数据是否合理
# 输出


我没有找到一篇近几年论文直接给出“Linux mainline EAS 下 LITTLE 核平均 idle 时间占比为 X%”这种可直接引用的数据。

最接近的研究是 WAEAS（2021）：

- 平台：RK3399，4×A53 LITTLE + 2×A72 big。
- Linux 4.4.83、Android 7.1.2。
- 作者观察到原 EAS 会把低负载任务较均匀地分散到 LITTLE 核，使多个 LITTLE 核持续 active、难以进入 idle。
- WAEAS 通过 task packing 把任务集中到较少 LITTLE 核，让其余核进入 idle。
- 但论文只报告能耗和性能，没有直接报告 LITTLE 核 idle 百分比。[WAEAS 论文](https://www.sciopen.com/article/10.26599/TST.2019.9010040)

这意味着你的课题是有价值的：

> 目前文献证明了“小任务分散会减少 LITTLE 核 idle”的现象，但缺少针对现代 mainline EAS 的定量 idle-residency 实验。


## 1. 找到的相关实验

### 1.1 WAEAS：最直接相关

论文明确指出，原始 EAS 对非延迟敏感的小任务倾向于选择利用率较低的 LITTLE 核，结果是任务在 LITTLE cluster 中分散，多个 LITTLE 核一直 active。其改进方法以 LITTLE 核容量的 80% 为 packing 阈值，把任务集中到少数核。[WAEAS 原文对应说明](https://peterhuistyping.github.io/CPU_Scheduling_Review/HMP/Initial-Draft-Productwise/WAEAS_An_optimization_scheme_of_EAS_scheduler_for_wearable_applications.pdf)

使用的应用包括：

- `perf bench sched messaging`
- `perf bench sched pipe`
- ParMiBench：
    - Bitcount
    - Susan
    - Basicmath
    - Patricia
    - Dijkstra
    - Stringsearch
    - SHA-1
- GPS、气压计、温湿度计、心率计等周期任务
- Wi-Fi 传输 4GB 文件

部分结果：

- Patricia：性能提高约 46%，功耗下降约 48%。
- 其他五个 ParMiBench 应用功耗平均改善约 5%。
- 低频传感器场景功耗改善约 10%。
- 但 SHA-1 因被持续压在 LITTLE 核，性能下降 13.87%、功耗反而恶化 10.16%。

这个反例很重要：更多 idle 并不保证更节能。若 task packing 导致运行时间明显延长，总能耗可能上升。

### 1.2 Linux EAS 合入实验

mainline EAS 的合入测试使用：

- Hikey960：4×A53 + 4×A73
- Juno r0：4×A53 + 2×A57
- Linux 4.19-rc5
- 10～50 个周期性 `rt-app` 任务
- 周期 16ms、占空比 5%、运行 30 秒、重复 10 次

Hikey960 能耗下降约 8%～22%，Juno 下降约 4%～27%；hackbench 性能影响通常在 0%～2% 左右。但该实验也没有发布 LITTLE 核 idle residency。[EAS v8 合入测试](https://lkml.rescloud.iu.edu/1810.2/00493.html)

### 1.3 Learning EAS

2020 年 Learning EAS 在 LG G8 ThinQ 等 Android big.LITTLE 平台上，相对 EAS 报告：

- 功耗改善 2.3%～7.8%；
- hackbench 改善 2.8%～25.5%；
- App 启动时间改善 2.2%～7.2%。

它重点研究负载预测和迁移代价，也没有报告每个 LITTLE 核的 idle 百分比。[论文页面](https://doaj.org/article/6f6603042ce3487da04e8ae67fc699b1)

---

# 2. 推荐的可复现实验

## 硬件选择

最推荐：

> RK3399 开发板，例如 RockPro64、NanoPC-T4 或其他有良好 mainline 支持的 RK3399 板。

原因：

- 4×Cortex-A53 LITTLE + 2×Cortex-A72 big，拓扑清楚；
- 与 WAEAS 使用同一 SoC，方便对比；
- mainline Device Tree 已提供：
    - LITTLE capacity：485
    - big capacity：1024
    - LITTLE 动态功耗系数：100
    - big 动态功耗系数：436
    - CPU_SLEEP 和 CLUSTER_SLEEP idle state

这些信息满足 EAS 所需的异构容量和 Energy Model 基础。[RK3399 Device Tree](https://kernel.googlesource.com/pub/scm/linux/kernel/git/s390/linux/+/4c7bcb51ae25f79e3733982e5d0cd8ce8640ddfc/arch/arm64/boot/dts/rockchip/rk3399.dtsi)

如果你已有 RK3588，也可以使用。它的 mainline Device Tree 同样包含 A55/A76 的容量和功耗系数，但板级固件、SCMI CPUFreq 和 cpuidle 支持需要先验证。

---

## Linux 版本

推荐：

> Linux 6.12.96 LTS

6.12 是支持到 2028 年的 LTS 系列；截至 2026 年 7 月，6.12.96 是最新维护版本之一。[kernel.org](https://www.kernel.org/index.html)

不建议直接复刻 Android 7.1.2/Linux 4.4，因为那套 EAS 使用 WALT、SchedTune 和大量 Android vendor 修改，与现代 mainline EAS 差异很大。

---

## 内核配置

从 `arm64 defconfig` 开始，确保：

```
CONFIG_SMP=y
CONFIG_ENERGY_MODEL=y

CONFIG_CPU_FREQ=y
CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
CONFIG_CPU_FREQ_DEFAULT_GOV_SCHEDUTIL=y
CONFIG_CPU_FREQ_STAT=y

CONFIG_CPU_IDLE=y
CONFIG_ARM_CPUIDLE=y
CONFIG_ARM_PSCI_CPUIDLE=y
CONFIG_NO_HZ_IDLE=y

CONFIG_PROC_SYSCTL=y
CONFIG_SCHED_DEBUG=y

CONFIG_FTRACE=y
CONFIG_TRACING=y
CONFIG_POWER_TRACER=y
CONFIG_IRQ_TIME_ACCOUNTING=y
```

现代 mainline 没有一个叫 `CONFIG_EAS` 的独立选项。EAS 主要在以下条件满足时运行：

- `CONFIG_ENERGY_MODEL=y`
- schedutil governor
- 异构 CPU capacity
- Energy Model 已注册
- 没有 SMT
- 支持 frequency-invariant utilization

这是 Linux 官方列出的运行要求。[Linux EAS 文档](https://docs.kernel.org/scheduler/sched-energy.html)

### 编译示例

```
git clone --depth 1 --branch v6.12.96 \
    https://github.com/torvalds/linux.git linux-6.12

cd linux-6.12

make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- defconfig

scripts/config --enable ENERGY_MODEL
scripts/config --enable CPU_FREQ
scripts/config --enable CPU_FREQ_GOV_SCHEDUTIL
scripts/config --enable CPU_FREQ_DEFAULT_GOV_SCHEDUTIL
scripts/config --enable CPU_FREQ_STAT
scripts/config --enable CPU_IDLE
scripts/config --enable ARM_CPUIDLE
scripts/config --enable ARM_PSCI_CPUIDLE
scripts/config --enable NO_HZ_IDLE
scripts/config --enable PROC_SYSCTL
scripts/config --enable SCHED_DEBUG
scripts/config --enable FTRACE
scripts/config --enable TRACING
scripts/config --enable POWER_TRACER

make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- olddefconfig

make -j"$(nproc)" \
    ARCH=arm64 \
    CROSS_COMPILE=aarch64-linux-gnu- \
    Image modules dtbs
```

内核、DTB 和模块的安装方式取决于开发板的 bootloader，因此建议从该板当前可启动的配置开始，而不是直接替换启动分区。

---

# 3. 确认 EAS 真的启用了

启动后执行：

```
uname -r

cat /sys/devices/system/cpu/cpu*/cpu_capacity

for p in /sys/devices/system/cpu/cpufreq/policy*; do
    cat "$p/scaling_governor"
done

cat /proc/sys/kernel/sched_energy_aware
```

预期：

```
cpu0-cpu3：较小容量，例如约 485
cpu4-cpu5：较大容量，例如 1024

所有 CPUFreq policy：schedutil

sched_energy_aware：1
```

Linux 6.12 已经提供：

```
sudo sysctl kernel.sched_energy_aware=1
sudo sysctl kernel.sched_energy_aware=0
```

因此可以在同一个内核、同一个 governor 下做 EAS 开/关 A/B 测试，不必通过切换 `performance` governor 间接关闭 EAS。这样避免把 EAS 效果和 DVFS 效果混在一起。[Linux 6.12 调度器源码](https://raw.githubusercontent.com/torvalds/linux/v6.12/kernel/sched/topology.c)

如果这个 sysctl 不存在，说明：

- 内核配置缺失；
- 或该发行版移除了接口；
- 或使用的不是对应 mainline 实现。

---

# 4. 应该运行哪些 App

## 第一组：核心实验，周期低负载

使用 `rt-app`：

- 任务数：1、2、4、8、10、20、30、40、50
- 周期：16ms
- 每周期运行：0.8ms
- 占空比：5%
- 总时间：30秒
- 调度策略：`SCHED_OTHER`
- 重复：至少 10 次

这组最适合研究 LITTLE 核 idle，因为它：

- 与 EAS 合入实验一致；
- 总负载可精确控制；
- 可以观察轻载时 EAS 是 spread 还是 pack；
- 能形成从空闲到 overutilized 的连续曲线。

`rt-app` 是 Linux 调度器官方文档推荐的可复现负载生成器，支持 `SCHED_OTHER`、FIFO、RR 和 DEADLINE。[rt-app 文档](https://www.kernel.org/doc/html/next/scheduler/sched-deadline.html#appendix-a-test-suite)

安装或编译：

```
sudo apt install rt-app
```

或：

```
git clone https://github.com/scheduler-tools/rt-app.git
cd rt-app
sudo apt install build-essential autoconf automake libtool libjson-c-dev
./autogen.sh
./configure
make -j"$(nproc)"
sudo make install
```

## 第二组：调度器压力

```
perf bench sched messaging --pipe --thread --group 1 --loop 50000
perf bench sched messaging --pipe --thread --group 2 --loop 50000
perf bench sched messaging --pipe --thread --group 4 --loop 50000
perf bench sched pipe
```

记录：

- 完成时间；
- context switch；
- migration；
- LITTLE idle；
- big idle。

## 第三组：可控 CPU 负载

安装：

```
sudo apt install stress-ng
```

或者从源码编译：

```
git clone https://github.com/ColinIanKing/stress-ng.git
cd stress-ng
make clean
make -j"$(nproc)"
```

它支持 ARM64，但项目自己说明它主要是压力工具，不应把 bogo-ops 当成精确性能 benchmark。[stress-ng 项目](https://github.com/ColinIanKing/stress-ng)

运行：

```
stress-ng --cpu 1 --cpu-method matrixprod --timeout 30s
stress-ng --cpu 2 --cpu-method matrixprod --timeout 30s
stress-ng --cpu 4 --cpu-method matrixprod --timeout 30s
stress-ng --cpu 6 --cpu-method matrixprod --timeout 30s
```

## 第四组：真实应用

建议选择：

```
openssl speed -multi 1 sha256
openssl speed -multi 4 sha256

7zz b -mmt1
7zz b -mmt4
7zz b -mmt6

ffmpeg -i input.mp4 -c:v libx264 -preset medium -threads 1 -f null -
ffmpeg -i input.mp4 -c:v libx264 -preset medium -threads 6 -f null -
```

这些分别代表：

- 单线程计算；
- 多线程计算；
- 编解码；
- 可扩展与不可扩展任务。

---

# 5. 正确计算 LITTLE 核 idle 占比

主指标不要只用 `top` 或 `/proc/stat`，而应使用 cpuidle residency：

```
CPU c 的 idle ratio
  = 所有 cpuidle state 的 Δtime 之和
    ÷ 实验墙钟时间
```

对于四个 LITTLE 核：

```
LITTLE cluster 平均 idle ratio
  = 所有 LITTLE 核的 idle 时间总和
    ÷ (4 × 实验墙钟时间)
```

Linux 在下面接口中给出每个 CPU、每个 idle state 的累计时间，单位为微秒：

```
/sys/devices/system/cpu/cpu<N>/cpuidle/state<M>/time
```

这是内核官方定义的 cpuidle 驻留时间接口。[CPUIdle 文档](https://www.kernel.org/doc/html/latest/admin-guide/pm/cpuidle.html)

还应分别报告：

- 总 idle ratio；
- 浅 idle，例如 WFI；
- 深 idle；
- 每秒 idle entry 次数；
- 平均每次 idle duration。

因为：

```
90% idle，但每次只睡几十微秒
```

和：

```
90% idle，连续进入深度 idle 数十毫秒
```

能耗意义完全不同。

### 用 trace 验证

```
sudo trace-cmd record \
    -e power:cpu_idle \
    -e sched:sched_switch \
    -e sched:sched_migrate_task \
    -- rt-app workload.json
```

`power:cpu_idle` 的正数 state 表示进入 idle，`-1` 表示退出当前 idle state。[Linux power tracepoint 文档](https://origin.kernel.org/doc/html/v6.13/trace/events-power.html)

---

# 6. 实验矩阵

每个 workload 做：

|配置|EAS|CPU affinity|
|---|---|---|
|A|开|全部 CPU|
|B|关|全部 CPU|
|C|不重要|LITTLE-only|
|D|不重要|big-only|

A/B 用于研究 EAS 本身；C/D 是性能与能耗上下界。

每项至少：

- 预热 30 秒；
- 正式运行 30～60 秒；
- 重复 10 次；
- 随机化 EAS on/off 运行顺序；
- 每轮之间冷却到相近温度；
- 关闭无关服务、网络和 GUI；
- 使用风扇固定散热条件；
- 同时记录环境温度、CPU 温度和频率。

输出至少包含：

```
workload
EAS on/off
runtime
throughput
LITTLE total idle %
LITTLE deep-idle %
big total idle %
context switches
migrations
average frequency
energy J
temperature
```

---

# 7. 我对预期数据的合理性审查

## 预期一：轻量周期任务

合理预期：

- 任务主要落在 LITTLE cluster；
- big 核 idle 很高；
- LITTLE 核总体仍有较多 idle；
- EAS on/off 的差异主要体现在任务是集中还是分散。

但不要先假定 EAS 一定让 LITTLE idle 更高。WAEAS 的观察恰恰表明，某些 EAS 实现会主动选择较空闲 LITTLE 核，导致多个核保持浅度 active。

## 预期二：一个持续 CPU-bound 任务

它的 PELT utilization 最终可能超过 LITTLE 核可提供容量，从而成为 misfit task，被迁移到 big 核。

因此可能看到：

```
big 核忙
LITTLE 核接近全 idle
```

这不是 EAS 失败，而是容量感知调度在保证性能。

## 预期三：CPU-bound 任务数达到总核数

此时 LITTLE idle 应接近 0。Linux 官方文档说明，当 CPU 超过约 80% capacity、系统进入 overutilized 状态后，EAS 会退出关键 wake-up 路径，传统 load balancing 重新工作。[EAS overutilization](https://docs.kernel.org/scheduler/sched-energy.html)

如果六个 CPU-bound worker 运行时 LITTLE idle 仍然很高，需要检查：

- affinity 是否限制；
- worker 是否真的 CPU-bound；
- thermal throttling；
- 有无线程阻塞；
- 某些 CPU 是否 offline。

## 预期四：idle 上升但能耗没有下降

这是完全可能的：

- idle 只是浅 WFI，而不是 cluster deep idle；
- task packing 提高了活动核频率；
- 运行时间延长；
- 频繁 wakeup 造成进入/退出开销；
- DRAM/GPU/外设功耗占主导。

因此“LITTLE idle 百分比”不能单独作为节能结论，必须和深 idle、运行时间及能量一起报告。

## 预期五：数据重复性

建议接受标准：

- runtime 的 RSD 小于 3%；
- idle ratio 的绝对标准差小于约 2～3 个百分点；
- 若超过，检查温度、后台 IRQ、网络和 governor；
- 报告均值、标准差和 95% CI，不只报告一次运行。

---

## 最终课题建议

可以把课题定成：

> **Quantifying LITTLE-core Idle Residency under Linux Energy-Aware Scheduling on ARM big.LITTLE**

核心研究问题：

1. 现代 mainline EAS 在低、中、高负载下会让 LITTLE 任务 spread 还是 pack？
2. EAS 对 LITTLE 总 idle、deep idle 和 wakeup frequency 各有什么影响？
3. LITTLE idle 增加是否真的对应能耗下降？
4. 哪类 workload 会出现“idle 更多，但能耗或性能反而更差”？

最值得先跑的第一组实验是：

```
RK3399
Linux 6.12.96
EAS on vs off
rt-app：1/2/4/8/10/20/30/40/50 个任务
16ms period，5% duty，30s
每项 10 次
采集 cpuidle residency + cpu_idle trace + runtime + energy
```

这套实验既能复刻 EAS 最初的官方负载，又直接补上现有论文缺失的 LITTLE-core idle 定量数据。

